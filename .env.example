# SQLite file-based database
# For local development: file:./dev.db
# For production on Vercel: file:/tmp/prod.db
# Note: /tmp is writable on Vercel but ephemeral — data resets on cold starts
DATABASE_URL="file:./dev.db"
